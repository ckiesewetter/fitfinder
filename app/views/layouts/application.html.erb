<!DOCTYPE html>
<html>
  <head>
    <!--These three scripts are for google maps -->
    <script src="//maps.google.com/maps/api/js?key=AIzaSyDd87aqSi8obY2R7coA5gqBnkajyKldrRM"></script>
    <script src="//cdn.rawgit.com/mahnunchik/markerclustererplus/master/dist/markerclusterer.min.js"></script>
    <script src="//cdn.rawgit.com/printercu/google-maps-utility-library-v3-read-only/master/infobox/src/infobox_packed.js" type="text/javascript"></script>
    <title>Fitfinder</title>
    <%= csrf_meta_tags %>
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <!-- Latest compiled and minified JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
  </head>

  <body id="<%= controller_name %>-<%= action_name %>" class="<%= controller_name %> <%= action_name %>">
      <div class="hidden-xs">
        <div class="logo-container">
          <a class="navbar-brand" href="#">
            <%= image_tag "find-fit-logo.png", alt: 'Find Fit' %>
          </a>
        </div>

        <div class="nav-container">
          <nav class="navbar navbar-default">
          <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
              <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
              <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                  <li class="active"><a href="#">About<span class="sr-only">(current)</span></a></li>
                  <li><a href="#">Contact</a></li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                  <li>
                    <!-- Add to Bootstrap header so is persistent across all pages -->
                    <% if controller.class.to_s.match('Devise') %>
                    <% elsif current_user.nil? %>
                      <%= link_to 'Log In', user_session_path, :id => "sign_in" %>
                    <% else  %>
                      <%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %>
                    <% end %>
                  </li>
                </ul>
              </div><!-- /.navbar-collapse -->
            </div><!-- /.container-fluid -->
          </nav>
        </div>
      </div>

      <div class="visible-xs-block">
        <div class="row">
          <div class="col-sm-12">
            <div class="logo-container-xs">
              <a class="navbar-brand" href="#">
                <%= image_tag "find-fit-logo.png", alt: 'Find Fit' %>
              </a>
            </div>
          </div>
        </div>

        <div class="row">
          <div class="col-sm-12">
            <div class="nav-container-xs">
            <nav class="navbar navbar-default">
            <div class="container-fluid">
              <!-- Brand and toggle get grouped for better mobile display -->
              <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                  <span class="sr-only">Toggle navigation</span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                </button>
              </div>

              <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                  <ul class="nav navbar-nav">
                    <li class="active"><a href="#">About<span class="sr-only">(current)</span></a></li>
                    <li><a href="#">Contact</a></li>
                  </ul>
                  <ul class="nav navbar-nav navbar-right">
                    <li>
                      <!-- Add to Bootstrap header so is persistent across all pages -->
                      <% if controller.class.to_s.match('Devise') %>
                      <% elsif current_user.nil? %>
                        <%= link_to 'Log In', user_session_path, :id => "sign_in" %>
                      <% else  %>
                        <%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %>
                      <% end %>
                    </li>
                  </ul>
                </div><!-- /.navbar-collapse -->
              </div><!-- /.container-fluid -->
            </nav>
          </div>
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="notice"><%= notice %></div>
      <div class="alert"><%= alert %></div>
      <%= yield %>
    </div>

  </body>
</html>
